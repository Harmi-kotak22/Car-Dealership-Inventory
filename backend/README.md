# Backend

This folder contains the backend skeleton for the car dealership inventory application.

## Getting started

1. Install dependencies with `npm install`.
2. Copy `.env.example` to `.env` if needed.
3. Start the server with `npm run dev`.
