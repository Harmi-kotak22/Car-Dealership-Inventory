# Frontend

This folder contains the frontend skeleton for the car dealership inventory application.

## Getting started

1. Install dependencies with `npm install`.
2. Start the development server with `npm run dev`.
